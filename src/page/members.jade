extends ../layout/base

append main
    .page-header
        h2 Members
    .row
        each item in members
            .col-xs-12.col-sm-6.col-md-3
                img.member.img-responsive.img-rounded(src=item.avatar)
                .caption
                    h3=item.name
                    h4=item.degree
                    
